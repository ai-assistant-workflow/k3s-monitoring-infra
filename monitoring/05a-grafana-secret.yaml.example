# 이 파일을 05a-grafana-secret.yaml 로 복사 후 실제 값으로 치환하여 사용.
# 실제 파일은 .gitignore에 의해 커밋되지 않습니다.

apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: monitoring
type: Opaque
stringData:
  admin-user: admin
  admin-password: __CHANGE_ME__
